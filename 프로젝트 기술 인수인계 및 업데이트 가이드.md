# 프로젝트 기술 인수인계 및 업데이트 가이드

본 문서는 AI 모델 또는 후임 개발자가 본 프로젝트의 핵심 로직을 이해하고 버전을 업데이트할 수 있도록 기술적인 세부 사항을 기술합니다.

## 1. 핵심 알고리즘 구조

### 1.1 엑셀 파싱 로직 (`validate_free_semester_plan_bytes`)
- **데이터 로드**: `openpyxl`의 `data_only=True` 모드를 사용하여 수식이 아닌 계산된 최종값을 읽어옵니다.
- **좌표 기반 추출**: 학교명, 시수 등 메타데이터는 표준 서식의 고정 셀 좌표(D4, D8, D9, E3 등)를 사용합니다.
- **상태 기반 순회(Stateful Iteration)**: 활동 시트 파싱 시, 첫 번째 열의 텍스트를 감지하여 현재 처리 중인 카테고리(주제선택, 진로탐색 등)를 추적합니다.

### 1.2 예산 수식 검증 (`evaluate_formula_string`)
- **정규식 정제**: `15,000원*12명*2회`와 같은 한글 텍스트 포함 수식에서 숫자와 연산자(`*`, `+`, `/`, `.`)만 추출합니다.
- **안전한 평가**: 추출된 문자열을 `eval()`을 통해 계산하여 엑셀에 입력된 소요예산과 비교합니다. (단수 차이 보정을 위해 10원 미만의 오차는 허용합니다.)

## 2. 데이터 모델 (Result Schema)
검토 결과는 아래와 같은 딕셔너리 구조로 관리됩니다:
```python
{
    "file": "파일명.xlsx",
    "pass_count": 8,
    "fail_count": 2,
    "summary": "총평 텍스트",
    "checks": [
        {
            "category": "영역명",
            "item": "점검항목명",
            "result": "PASS/FAIL",
            "detail": "상세 계산 내역 및 오류 사유"
        }
    ]
}
```

## 3. 버전 업데이트 시 주의사항 (Maintenance)

### 3.1 서식 개정 시 대응
교육청 서식이 변경되어 셀 위치가 달라질 경우, `app.py` 내의 다음 좌표들을 수정해야 합니다:
- `1.학교운영 현황` 시트: `D8`(주제선택), `D9`(진로탐색), `B11`(학급수 키워드 위치)
- `3. 예산 계획서` 시트: `E3`(총 예산액)

### 3.2 검증 규칙(Rule) 추가 방법
`validate_free_semester_plan_bytes` 함수 내부에 새로운 조건을 추가하고, `results["checks"].append()`를 통해 결과를 기록하면 UI와 엑셀 다운로드에 자동으로 반영됩니다.

### 3.3 스타일링 수정
- **UI 강조**: `highlight_fail` 함수를 수정하여 색상이나 폰트 스타일을 변경할 수 있습니다.
- **엑셀 서식**: `writer.sheets[sheet_name]` 객체를 통해 `openpyxl`의 스타일링 API(Alignment, Font 등)를 추가 적용할 수 있습니다.

## 4. AI 활용 가이드 (For AI Prompts)
다른 AI에게 업데이트를 요청할 때 다음 프롬프트를 활용하세요:
> "첨부된 app.py는 자유학기 계획서 검토 웹앱이야. [특정 조건, 예: 동아리 활동 시수 검증] 기능을 추가해줘. 결과는 results['checks'] 리스트에 기존과 동일한 형식으로 추가하고, FAIL 발생 시 상세 사유를 detail에 적어줘."

---
* 최종 업데이트: 2026-08-17
* 작성자: Manus AI
